import Layout from '@components/layout/Rules/Layout';
import { type Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Rules',
	description: 'Community Guidelines - A Thing',
};

export default function Rules() {
	return <Layout />;
}
